# DumbCrawler Environment Variables
# Copy this file to .env and fill in your values

# Supabase credentials (same as your delorme-os2 app)
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# API URL - your deployed delorme-os2 application
# If using Cloudflare Workers, it will look like:
API_URL=https://your-app.your-subdomain.workers.dev

# Or if you have a custom domain:
# API_URL=https://app.yourdomain.com

# ===== SECURITY CONFIGURATION =====

# Crawler API Key (REQUIRED - must be at least 32 characters)
# Generate with: openssl rand -base64 32
API_KEY=your-secure-api-key-min-32-chars-here

# CORS Allowed Origins (comma-separated list of allowed frontend URLs)
ALLOWED_ORIGINS=https://your-app.your-subdomain.workers.dev

# ===== VPS DEPLOYMENT (if deploying to remote server) =====

# VPS connection details
VPS_HOST=your-vps-ip-address
VPS_USER=your-vps-username

# IMPORTANT: Use SSH key-based authentication - NEVER store passwords
# Generate SSH key: ssh-keygen -t ed25519 -C "your-email@example.com"
# Add public key to VPS: ssh-copy-id user@vps-ip-address
# Password authentication should be disabled on the VPS
